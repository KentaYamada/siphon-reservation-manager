/**
 * Reservation resend mail entity
 */
export interface ReservationResendMail {
  reservation_id: string;
  reservation_date_id: string;
  reservation_time_id: string;
  reserver_name: string;
  tel: string;
}
