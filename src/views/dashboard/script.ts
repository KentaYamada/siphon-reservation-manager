import Vue from "vue";
import UpdateList from "@/components/updates/list/UpdateList.vue";

/**
 * Dashboard view
 */
export default Vue.extend({
  name: "dashboard",
  components: {
    UpdateList
  }
});
